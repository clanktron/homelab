apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: carbide-registry-dashboard
spec:
  interval: 5m
  chart:
    spec:
      chart: ./chart
      sourceRef:
        kind: GitRepository
        name: carbide-registry-dashboard
        namespace: flux-system
      interval: 5m
  install:
    remediation:
      retries: 3
