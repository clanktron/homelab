apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-sqlite
  labels:
    app: mealie
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
---

