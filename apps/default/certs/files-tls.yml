---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: files-tls
spec:
  secretName: files-tls
  dnsNames:
    - files.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
