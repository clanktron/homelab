---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: code-tls
spec:
  secretName: code-tls
  dnsNames:
    - code.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io