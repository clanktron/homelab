---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pve-tls
spec:
  secretName: pve-tls
  dnsNames:
    - pve.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io