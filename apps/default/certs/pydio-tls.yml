---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pydio-tls
spec:
  secretName: pydio-tls
  dnsNames:
    - pydio.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io