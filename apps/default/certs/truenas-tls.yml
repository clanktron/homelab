---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: truenas-tls
spec:
  secretName: truenas-tls
  dnsNames:
    - truenas.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io