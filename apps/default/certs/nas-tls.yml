---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nas-tls
spec:
  secretName: nas-tls
  dnsNames:
    - nas.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io