apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "arr.fullname" . }}-downloads
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.downloads.storage }}
