{{- if .Values.prowlarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arr.fullname" . }}-prowlarr
  labels:
    {{- include "arr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.prowlarr.service.type }}
  ports:
    - port: {{ .Values.prowlarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arr.selectorLabels" . | nindent 4 }}-prowlarr
{{- end }}
