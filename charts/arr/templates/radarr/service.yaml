{{- if .Values.radarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arr.fullname" . }}-radarr
  labels:
    {{- include "arr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.radarr.service.type }}
  ports:
    - port: {{ .Values.radarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arr.selectorLabels" . | nindent 4 }}-radarr
{{- end }}
