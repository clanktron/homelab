{{- if .Values.jellyseerr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arr.fullname" . }}-jellyseerr
  labels:
    {{- include "arr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.jellyseerr.service.type }}
  ports:
    - port: {{ .Values.jellyseerr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arr.selectorLabels" . | nindent 4 }}-jellyseerr
{{- end }}
